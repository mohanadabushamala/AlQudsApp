<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.alqudsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThemeAlQudsApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NumberAlQudsActivity"/>
        <activity android:name=".PhotoAlQudsActivity" />
        <activity android:name=".LastNewsActivity"/>
        <activity android:name=".SettingsActivity"/>
        <activity android:name=".AlQudsActivity" />
        <activity android:name=".MainActivity"/>

        <service
            android:name=".MyService2"
            android:enabled="true"
            android:exported="true"
            tools:ignore="Instantiatable"></service>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

</manifest>